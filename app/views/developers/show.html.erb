<div class="row">
  <div class="col-md-12">
<p>
  <strong>Developer Name:</strong> <%= @developer.name %>
</p>
<p>
  <%= image_tag @developer.photo.url(:large), class: "" %>  
</p>
<% if current_user %>
<%= link_to "EDIT", edit_developer_path(@developer) %>
<%= link_to "DELETE", developer_path(@developer), method: :delete, data: {confirm: "Are you sure you want to delete this developer?"}%>
<% end %>
</div>
</div>
<hr />


<div class="row">    
    <h3>Posts</h3>

    <% @posts.each do |post| %>
    <div class="col-md-4 post">
      <%= link_to post_path(post) do %>
      <div>
      <h4><%= post.title %></h4>
      <p><%= image_tag post.photo.url(:medium), class: "" %></p>
      <p><%= post.body %></p>
      <p>on <%= post.created_at.to_formatted_s(:long) %></p>
      </div>
      <% end %>
      <h6>Comments</h6>
      <% @comments = post.comments %>
      <% @comments.each do |comment| %>
      <p><%= comment.body %>
      by: <%= "#{comment.user.email}"+' on '+"#{comment.created_at.to_formatted_s(:long)}" %></p>
        <% end %>
      </div>
      <% end %>
  </div>
</div>